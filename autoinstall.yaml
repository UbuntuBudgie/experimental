# https://github.com/canonical/subiquity
version: 1
identity:
    realname: ''
    hostname: hostname
    password: "$6$SFSJImkPPNDMWnW1$AGxOm4TudWtN155NmI2B/BNdjs7E0.jo2JAf8tJF.KDtIBTlv3n7YJ2EkRb0JoG2VOnSAszrynJUE.U8JSu2j."
    username: ubuntu
locale: en_GB.UTF-8
keyboard:
  layout: gb
source:
  id: ubuntu-budgie-desktop-minimal
packages:
  - remmina
late-commands:
  - curtin in-target --target=/target -- snap remove --purge --no-wait ubuntu-budgie-welcome || true
  - curtin in-target --target=/target -- snap remove --purge --no-wait snap-store || true
  - curtin in-target --target=/target -- snap remove --purge --no-wait snapd-desktop-integration || true
  - curtin in-target --target=/target -- apt-get -y --quiet=2 remove onboard magnus  || true
  - curtin in-target --target=/target -- apt-get -y --quiet=2 autoremove || true
updates: all
timezone: Europe/London
storage:
    layout:
      name: lvm
      password: password
      match:
        size: largest
